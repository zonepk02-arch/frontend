import { Logger } from 'html2canvas/dist/types/core/logger';
import { Cache, ResourceOptions } from 'html2canvas/dist/types/core/cache-storage';
import { Bounds } from 'html2canvas/dist/types/css/layout/bounds';
export declare type ContextOptions = {
    logging: boolean;
    cache?: Cache;
} & ResourceOptions;
export declare class Context {
    windowBounds: Bounds;
    private readonly instanceName;
    readonly logger: Logger;
    readonly cache: Cache;
    private static instanceCount;
    constructor(options: ContextOptions, windowBounds: Bounds);
}
